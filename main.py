
#create function that takes the hashmap and the parsed dictionary for type - this will be driver function
  #for loop to go through each row and call endpoint functino
     # id = column_fj_func_from_below
     # create chemical object
     # hashmap.add (id, chem_obj)

#func that calls flapjack endpoint for submitting column - input will be data submitted to fj and output will be id